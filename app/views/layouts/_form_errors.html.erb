<% if model.errors.any? %>
<div class="my-4">

<h4 class="text-danger"><%= model.errors.count %>件のエラーが発生しました。</h4>
<ul>
  <% model.errors.full_messages.each do |message| %>
  <li><%= message %></li>
  <% end %>
</ul>

</div>
<% end %>