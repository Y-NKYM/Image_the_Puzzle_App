<div class="text-right">
  <% if post.bookmarked_by?(current_user) %>
  	<h4>
  		<%= link_to post_bookmarks_path(post.id), method: :delete, remote: true do %>
  			★
  		<% end %>
  	</h4>
  <% else %>
  	<h4>
  		<%= link_to post_bookmarks_path(post.id), method: :post, remote: true do %>
  			☆
  		<% end %>
  	</h4>
  <% end %>
</div>