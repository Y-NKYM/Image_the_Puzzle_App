<div class="container margin-y w-75">
  <%= render 'admin/searches/admin_search_form' %>
</div>

<div class="container">
  <h2>管理者側：投稿一覧</h2>
  <div class="mx-auto margin-y">
    <%= render 'admin/posts/admin_post_form', posts: @posts %>
  </div>
  <div class="margin-y">
    <%= paginate @posts, window: 1, left: 1, right: 1 %>
  </div>
</div>